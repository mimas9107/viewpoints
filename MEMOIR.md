----
name:          "MEMOIR.md"
description:   "開發紀實與技術決策紀錄"
created_date:  "2026/05/07 12:30:00"
modified_date: "2026/05/07 12:30:00"
project_version: "2.2.4"
document_version: "1.0.0"
agent_sign: ['AI Agent Framework', 'gemini cli/mimas']
----

# 📝 Viewpoints 開發紀實 (Project Memoir)

本文件紀錄了專案開發過程中的重大技術決策、架構演進以及疑難雜症的解決方案。

---

## 📱 [v2.2.4] 手機端體驗大重構：行動優先 (Mobile First) 之戰

### 1. 問題背景
在 v2.2.3 之前的版本中，使用者回報手機端存在以下嚴重問題：
- 監視牆無法向下捲動（被 `overflow: hidden` 鎖死）。
- 按鈕文字在窄螢幕下會垂直擠壓。
- JS 動態注入的格狀佈局（如 2x2）會覆蓋手機端的回應式設定。
- 手機瀏覽器快取舊 CSS 導致修改無法立即生效。

### 2. 技術關鍵點與解決方案

#### A. 思維轉向：行動優先 (Mobile First)
- **決策**：放棄「修補電腦版樣式給手機看」的做法，改為「預設即手機版」。
- **實作**：全域樣式預設為單欄佈局與開放捲動，只有在大螢幕 (>1025px) 才啟用格狀佈局與捲動限制。這從根本上排除了權限（Specificity）衝突。

#### B. 快取刷新 (Cache Busting)
- **決策**：解決手機瀏覽器「頑強」的快取機制。
- **實作**：在 HTML 引用 CSS 時加入版本參數：`css/style.css?v=2.2.4`。

#### C. 強制權限覆蓋 (Specificity Enforcement)
- **決策**：解決 JS 動態注入類別導致的佈局崩壞。
- **實作**：在手機媒體查詢中，明確列出所有可能的 JS 類別（如 `.camera-grid.grid-3x3`）並加上 `!important` 進行單欄重設。

### 3. 前後對比 (v2.2.1 vs v2.2.4)

| 特性 | 修正前 (v2.2.1) | 修正後 (v2.2.4) |
| :--- | :--- | :--- |
| **佈局策略** | 電腦版為主 (Desktop-First) | **行動優先 (Mobile First)** |
| **監視器排列** | 手機端會嘗試維持格狀，導致擠壓 | **強制 100% 寬度單欄排列** |
| **捲動權限** | 全域鎖死 `overflow: hidden` | **手機端開放式垂直捲動** |
| **按鈕列** | 容易換行或文字垂直擠壓 | **強制不換行水平滑動列** |
| **快取處理** | 依賴瀏覽器刷新，通常無效 | **主動刷新標記 (?v=2.2.4)** |

### 4. 學習與啟發
行動網頁開發中最常見的陷阱不是「如何實現」，而是「如何撤銷電腦版的設定」。採用行動優先的寫法，可以讓我們從最簡單的流動佈局開始，逐步向大螢幕疊加複雜度，這比反向操作要穩定得多。
